A basic widget made using Tkinter framework in python where you can encrypt or decrypt messages. the masterpassword acts as the key for encrypting and decrypting. the messages are encrypted by using hash and Fernet key
NOTE:SET THE MASTER PASSWORD AND SALT VARIABLE BY YOURSELF.
